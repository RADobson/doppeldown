---
phase: 08-semantic-colors-refactor
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/empty-state.tsx
autonomous: true

must_haves:
  truths:
    - "UI components use semantic color classes (bg-card, text-foreground, border-border)"
    - "No hardcoded gray colors remain in UI component files"
    - "UI components render identically to before migration"
    - "Button variants still function correctly (primary, secondary, ghost, etc.)"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card component with semantic colors"
      contains: "bg-card"
    - path: "src/components/ui/button.tsx"
      provides: "Button component with semantic colors"
      contains: "bg-muted"
    - path: "src/components/ui/input.tsx"
      provides: "Input component with semantic colors"
      contains: "border-input"
  key_links:
    - from: "src/components/ui/*.tsx"
      to: "tailwind.config.ts"
      via: "Semantic class names"
      pattern: "bg-card|bg-muted|text-foreground|border-border"
---

<objective>
Migrate all UI components from hardcoded Tailwind colors to semantic tokens.

Purpose: UI components are the foundation used across all pages. Migrating them first ensures consistent styling and reduces migration scope for page-level work.

Output: 6 UI component files using semantic color classes instead of hardcoded gray values.
</objective>

<execution_context>
@/home/dobsondev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dobsondev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-semantic-colors-refactor/08-RESEARCH.md

@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/badge.tsx
@src/components/ui/skeleton.tsx
@src/components/ui/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Card component</name>
  <files>src/components/ui/card.tsx</files>
  <action>
Replace hardcoded colors in card.tsx with semantic tokens.

Mapping (from research):
- `bg-white` -> `bg-card`
- `border-gray-200` -> `border-border`
- `text-gray-900` -> `text-card-foreground`
- `text-gray-500` -> `text-muted-foreground`
- `bg-gray-50` -> `bg-muted`

Apply to each component:
- Card: bg-white -> bg-card, border-gray-200 -> border-border
- CardHeader: border-gray-200 -> border-border
- CardTitle: text-gray-900 -> text-card-foreground
- CardDescription: text-gray-500 -> text-muted-foreground
- CardFooter: border-gray-200 -> border-border, bg-gray-50 -> bg-muted
  </action>
  <verify>
Run `grep -E "gray-|bg-white" src/components/ui/card.tsx` - should return 0 matches.
Run `grep -E "bg-card|border-border|text-card-foreground|text-muted-foreground|bg-muted" src/components/ui/card.tsx` - should return 6+ matches.
  </verify>
  <done>
Card component uses semantic color tokens.
No hardcoded gray or white colors remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Button component</name>
  <files>src/components/ui/button.tsx</files>
  <action>
Replace hardcoded colors in button.tsx with semantic tokens.

Current variants:
- secondary: bg-gray-100 text-gray-900 hover:bg-gray-200 -> bg-muted text-foreground hover:bg-accent
- ghost: text-gray-600 hover:bg-gray-100 -> text-muted-foreground hover:bg-accent
- outline: border-gray-300 text-gray-700 hover:bg-gray-50 -> border-border text-foreground hover:bg-muted

Keep primary and danger variants unchanged (they use specific brand colors).

Update focus-visible:ring-gray-500 references:
- secondary: focus-visible:ring-gray-500 -> focus-visible:ring-ring
- ghost: focus-visible:ring-gray-500 -> focus-visible:ring-ring
  </action>
  <verify>
Run `grep -E "gray-" src/components/ui/button.tsx` - should return 0 matches.
Run `grep -E "bg-muted|text-foreground|border-border|hover:bg-accent" src/components/ui/button.tsx` - should return 4+ matches.
  </verify>
  <done>
Button component variants use semantic color tokens.
Primary and danger variants preserved with brand colors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate Input, Badge, Skeleton, EmptyState components</name>
  <files>
src/components/ui/input.tsx
src/components/ui/badge.tsx
src/components/ui/skeleton.tsx
src/components/ui/empty-state.tsx
  </files>
  <action>
Migrate remaining UI components using consistent mapping:

**input.tsx:**
- border-gray-300 -> border-input
- text-gray-900 -> text-foreground
- text-gray-400 (placeholder) -> text-muted-foreground
- text-gray-500 (label/helper) -> text-muted-foreground
- focus:ring-* / focus:border-* -> keep primary colors (brand)
- bg-gray-50 (disabled) -> bg-muted

**badge.tsx:**
- bg-gray-100 text-gray-800 -> bg-muted text-foreground (for default/gray variant)
- Keep specific color variants (red, yellow, green, blue) for semantic meaning
- Only replace generic gray usage

**skeleton.tsx:**
- bg-gray-200 -> bg-muted
- Keep animate-pulse unchanged

**empty-state.tsx:**
- bg-gray-100 -> bg-muted
- text-gray-600 -> text-muted-foreground
- text-gray-500 -> text-muted-foreground

IMPORTANT: Keep intentional color usage for semantic meaning (e.g., red for errors, green for success).
  </action>
  <verify>
Run for each file:
- `grep -c "gray-" src/components/ui/input.tsx` -> 0
- `grep -c "gray-" src/components/ui/skeleton.tsx` -> 0
- `grep -c "gray-" src/components/ui/empty-state.tsx` -> 0

For badge.tsx, only generic gray should be replaced - colored variants remain.

Run `npm run dev` and visually verify:
- Card component renders with correct background and borders
- Button variants display correctly (hover states work)
- Input fields have proper border and focus styling
- Skeleton shows muted background with pulse animation
  </verify>
  <done>
All UI components migrated to semantic tokens.
Semantic colors (red, green, yellow, blue for status) preserved intentionally.
No generic gray-* classes remain in UI components.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -rE "bg-white|text-gray-[0-9]|bg-gray-[0-9]|border-gray-[0-9]" src/components/ui/` returns minimal/zero results
2. `npm run build` succeeds
3. `npm run dev` - visual inspection: cards, buttons, inputs render with correct colors (same as before)
</verification>

<success_criteria>
- All 6 UI component files use semantic color classes
- No hardcoded gray-* colors in generic UI elements
- Button variants function correctly
- Card renders with proper background, borders, text
- Input shows correct border, focus states
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-semantic-colors-refactor/08-02-SUMMARY.md`
</output>
