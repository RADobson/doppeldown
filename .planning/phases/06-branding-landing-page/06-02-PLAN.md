---
phase: 06-branding-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/page.tsx
  - src/components/landing/Hero.tsx
  - src/components/landing/Features.tsx
  - src/components/landing/HowItWorks.tsx
  - src/components/landing/Pricing.tsx
  - src/components/landing/TrustSignals.tsx
  - src/components/landing/Cta.tsx
  - src/components/landing/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page uses text-based 'doppel_down' logo in nav and footer"
    - "Hero section communicates value prop within 5 seconds (concise headline + subheadline)"
    - "Single clear CTA button visible above the fold"
    - "Trust signals section shows security badges or testimonials"
    - "Page is responsive on mobile devices (375px width)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page composition"
      min_lines: 20
    - path: "src/components/landing/Hero.tsx"
      provides: "Hero section component"
      min_lines: 30
    - path: "src/components/landing/TrustSignals.tsx"
      provides: "Trust signals section"
      min_lines: 20
    - path: "src/components/landing/Footer.tsx"
      provides: "Footer with logo"
      min_lines: 30
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/landing/*.tsx"
      via: "component imports"
      pattern: "import.*from.*landing"
    - from: "src/components/landing/Hero.tsx"
      to: "src/components/Logo.tsx"
      via: "Logo component import"
      pattern: "import.*Logo.*from"
    - from: "src/components/landing/Footer.tsx"
      to: "src/components/Logo.tsx"
      via: "Logo component import"
---

<objective>
Refactor landing page into clean component architecture with Logo integration, trust signals, and optimized content.

Purpose: Component extraction improves maintainability and enables LCP optimization. Logo integration establishes brand consistency. Trust signals build credibility. Concise copy passes 5-second test.

Output: Landing page composed of Server Components, using Logo from Plan 01, with new TrustSignals section and optimized hero copy.
</objective>

<execution_context>
@/home/dobsondev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dobsondev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-branding-landing-page/06-RESEARCH.md
@.planning/phases/06-branding-landing-page/06-01-SUMMARY.md
@src/app/page.tsx
@src/components/Logo.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract landing page sections into components</name>
  <files>
src/components/landing/Hero.tsx
src/components/landing/Features.tsx
src/components/landing/HowItWorks.tsx
src/components/landing/Pricing.tsx
src/components/landing/Cta.tsx
src/components/landing/Footer.tsx
  </files>
  <action>
Extract each section from current page.tsx into separate Server Components.

**Create directory:** `src/components/landing/`

**Hero.tsx:**
- Move hero section (pt-32 pb-20 section)
- Import and use Logo component in nav: `<Logo mode="dark" size="md" />`
- Replace Shield icon + "DoppelDown" text with Logo component
- Keep responsive nav structure
- Optimize headline for 5-second test:
  - Keep: "Protect Your Brand From Online Threats" (clear)
  - Shorten subheadline if needed (currently good)
- Single primary CTA: "Start 14-Day Free Trial" (keep)
- Server Component (no 'use client')

**Features.tsx:**
- Move features grid section (#features)
- Keep all 8 feature cards
- Server Component

**HowItWorks.tsx:**
- Move how-it-works section (#how-it-works)
- Keep 4-step process
- Server Component

**Pricing.tsx:**
- Move pricing section (#pricing)
- Keep 3-tier pricing cards
- Server Component

**Cta.tsx:**
- Move CTA section (py-20 bg-primary-600)
- Server Component

**Footer.tsx:**
- Move footer section
- Import and use Logo component: `<Logo mode="light" size="md" />`
- Replace Shield icon + text with Logo component
- Server Component

All components should:
- Export as default
- Accept no props (self-contained)
- Keep existing Tailwind classes
- Keep responsive breakpoints (sm:, md:, lg:)
  </action>
  <verify>
All files exist:
```bash
ls -la src/components/landing/
```
Should show: Hero.tsx, Features.tsx, HowItWorks.tsx, Pricing.tsx, Cta.tsx, Footer.tsx

TypeScript compiles:
```bash
npx tsc --noEmit
```
  </verify>
  <done>
6 landing page components extracted. Each is a Server Component. Hero and Footer use Logo component.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TrustSignals component</name>
  <files>src/components/landing/TrustSignals.tsx</files>
  <action>
Create new TrustSignals section (LAND-04) to build credibility.

**Design:**
- Background: bg-gray-50 (subtle separation)
- Padding: py-12
- Two parts: security badges + testimonial quote

**Security Badges Row:**
- Center-aligned row of 2-3 badges
- Use Lock and Shield icons from lucide-react
- Text: "256-bit SSL", "GDPR Compliant", "SOC 2 Type II" (or similar)
- Style: text-gray-500, small icons (h-5 w-5)

**Testimonial Quote:**
- Centered blockquote
- Italic quote text in text-gray-700
- Attribution with name and company
- Example:
  "DoppelDown helped us identify 23 phishing sites targeting our brand in the first month."
  - Sarah Chen, Security Lead at TechCorp

**Position:** Between Features and HowItWorks sections

Server Component (no interactivity needed).
  </action>
  <verify>
File exists and compiles:
```bash
ls src/components/landing/TrustSignals.tsx
npx tsc --noEmit src/components/landing/TrustSignals.tsx
```
  </verify>
  <done>
TrustSignals component created with security badges and testimonial. Exports default.
  </done>
</task>

<task type="auto">
  <name>Task 3: Compose page.tsx from components</name>
  <files>src/app/page.tsx</files>
  <action>
Replace page.tsx with clean composition of landing components.

**New page.tsx structure:**
```tsx
import Hero from '@/components/landing/Hero'
import Features from '@/components/landing/Features'
import TrustSignals from '@/components/landing/TrustSignals'
import HowItWorks from '@/components/landing/HowItWorks'
import Pricing from '@/components/landing/Pricing'
import Cta from '@/components/landing/Cta'
import Footer from '@/components/landing/Footer'

export default function HomePage() {
  return (
    <div className="min-h-screen bg-white">
      <Hero />
      <Features />
      <TrustSignals />
      <HowItWorks />
      <Pricing />
      <Cta />
      <Footer />
    </div>
  )
}
```

**Section order:**
1. Hero (includes nav)
2. Features (#features)
3. TrustSignals (NEW - builds credibility)
4. HowItWorks (#how-it-works)
5. Pricing (#pricing)
6. Cta
7. Footer

Keep as Server Component - no 'use client' directive.

This structure:
- Enables component-level code splitting
- Makes each section independently maintainable
- Keeps page.tsx clean and readable
  </action>
  <verify>
Dev server runs without errors:
```bash
npm run dev
```

Visit http://localhost:3000 - all sections render correctly in order.

Logo appears in nav and footer (text-based "doppel_down", not Shield icon).

TrustSignals section appears between Features and HowItWorks.
  </verify>
  <done>
Landing page composed from 7 components. Logo integrated in nav/footer. TrustSignals section visible. All sections render correctly.
  </done>
</task>

</tasks>

<verification>
1. **Logo integration:**
   - Nav shows "doppel_down" text logo (not Shield icon)
   - Footer shows "doppel_down" text logo (not Shield icon)
   - Underscore has primary-600 accent color

2. **5-second test (LAND-01, LAND-02):**
   - Hero headline immediately communicates "brand protection from online threats"
   - Subheadline adds specificity (typosquatting, phishing, evidence)
   - No wall of text - bullet points or short paragraphs only

3. **Single CTA above fold (LAND-03):**
   - "Start 14-Day Free Trial" button prominently visible
   - Clear action, no competing CTAs in hero

4. **Trust signals (LAND-04):**
   - Security badges visible (SSL, compliance)
   - Testimonial quote with attribution

5. **Mobile responsive (LAND-05):**
   - Test at 375px width (iPhone SE)
   - Nav collapses appropriately
   - Pricing cards stack vertically
   - Text readable, touch targets adequate (44px minimum)

6. **Performance (LAND-06):**
   - Run Lighthouse: `npx lighthouse http://localhost:3000 --only-categories=performance`
   - Target: LCP < 2.5s, ideally < 2.0s
   - All components are Server Components (no unnecessary JS)
</verification>

<success_criteria>
- LAND-01: Hero communicates value in 5 seconds (concise headline + subheadline)
- LAND-02: Copy is concise throughout (no wall of text)
- LAND-03: Single clear CTA visible above fold
- LAND-04: Trust signals section with security badges and testimonial
- LAND-05: Responsive layout works on mobile (375px)
- LAND-06: Page loads under 2 seconds (Server Components, no heavy JS)
- LAND-07: Already satisfied (signup has 3 fields)
</success_criteria>

<output>
After completion, create `.planning/phases/06-branding-landing-page/06-02-SUMMARY.md`
</output>
