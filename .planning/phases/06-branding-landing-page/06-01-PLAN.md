---
phase: 06-branding-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Logo.tsx
  - src/app/favicon.ico
  - src/app/icon.svg
  - src/app/apple-icon.png
  - src/app/opengraph-image.png
  - src/app/twitter-image.png
  - src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Text-based 'doppel_down' logo renders with underscore accent color"
    - "Logo has light and dark mode variants"
    - "Favicon appears in browser tab"
    - "OG image displays when URL shared on social platforms"
  artifacts:
    - path: "src/components/Logo.tsx"
      provides: "Reusable logo component"
      exports: ["Logo"]
      min_lines: 20
    - path: "src/app/favicon.ico"
      provides: "Browser tab icon"
    - path: "src/app/icon.svg"
      provides: "Modern browser icon"
    - path: "src/app/apple-icon.png"
      provides: "Apple touch icon"
    - path: "src/app/opengraph-image.png"
      provides: "Social sharing preview"
    - path: "src/app/twitter-image.png"
      provides: "Twitter card image"
  key_links:
    - from: "src/app/layout.tsx"
      to: "favicon.ico, icon.svg, apple-icon.png"
      via: "Next.js file-based metadata (auto-detected)"
    - from: "src/app/layout.tsx"
      to: "opengraph-image.png, twitter-image.png"
      via: "Next.js file-based metadata (auto-detected)"
---

<objective>
Create distinctive brand identity with text-based logo and complete favicon/social sharing asset suite.

Purpose: Establishes visual brand identity that will be used across all pages. Logo component enables consistent branding; favicon/OG images enable professional appearance in browser tabs and social shares.

Output: Logo.tsx component with light/dark variants, favicon suite (ico, svg, apple-icon), OG/Twitter images, enhanced layout metadata.
</objective>

<execution_context>
@/home/dobsondev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dobsondev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-branding-landing-page/06-RESEARCH.md
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Logo component with light/dark variants</name>
  <files>src/components/Logo.tsx</files>
  <action>
Create a text-based Logo component with these specifications:

1. Text: "doppel_down" (lowercase, underscore between words)
2. Underscore character styled with primary-600 color as accent
3. Props interface:
   - mode: 'light' | 'dark' (default: 'dark' for dark text on light bg)
   - size: 'sm' | 'md' | 'lg' (controls font size)
   - className?: string (for additional styling)

4. Size mapping:
   - sm: text-lg (18px)
   - md: text-xl (20px)
   - lg: text-2xl (24px)

5. Color mapping:
   - mode='dark': text-gray-900 (for light backgrounds)
   - mode='light': text-white (for dark backgrounds)
   - Underscore always: text-primary-600

6. Base styling: font-bold tracking-tight

Use cn() utility from lib/utils if available, otherwise use template literals for className concatenation.

Example output:
```tsx
<span className="font-bold tracking-tight text-xl text-gray-900">
  doppel<span className="text-primary-600">_</span>down
</span>
```
  </action>
  <verify>
TypeScript compiles without errors: `npx tsc --noEmit src/components/Logo.tsx`
  </verify>
  <done>
Logo component exports correctly with mode and size props working. Dark mode shows white text, light mode shows gray-900 text, underscore always primary-600.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate favicon and social sharing assets</name>
  <files>
src/app/favicon.ico
src/app/icon.svg
src/app/apple-icon.png
src/app/opengraph-image.png
src/app/twitter-image.png
  </files>
  <action>
Create branding assets programmatically. Since we need simple text-based assets, generate them inline.

1. **favicon.ico** (multi-size: 16x16, 32x32, 48x48)
   - Use an online favicon generator OR create minimal placeholder
   - For MVP: Create a simple "D" or "dd" text favicon
   - Recommended: Use https://favicon.io/favicon-generator/ with text "DD", font Inter, background primary-600

2. **icon.svg** (scalable)
   - Create SVG with "doppel_down" text or "DD" monogram
   - Use primary-600 (#4f46e5 or similar) as accent color
   - Keep it simple and recognizable at small sizes

3. **apple-icon.png** (180x180)
   - Same design as favicon but 180x180 PNG
   - Solid background color with logo text or monogram

4. **opengraph-image.png** (1200x630)
   - Background: Dark (gray-900 or similar)
   - Center: "doppel_down" logo text (white with primary underscore)
   - Tagline below: "Take Down Brand Impostors"
   - Keep design simple and readable at small preview sizes

5. **twitter-image.png** (1200x630)
   - Same as OG image (can be identical file)

For programmatic generation, use Next.js ImageResponse API or create static files manually. Static files are simpler for this use case.

IMPORTANT: These files go in /src/app/ directory (not /public/) for Next.js file-based metadata to auto-detect them.

Simplest approach: Download/create placeholder images now, iterate on design later. A basic text-based image is better than no image.
  </action>
  <verify>
Files exist:
- `ls -la src/app/favicon.ico src/app/icon.svg src/app/apple-icon.png src/app/opengraph-image.png src/app/twitter-image.png`

Favicon appears in browser: Start dev server, check browser tab shows custom icon (not default Next.js icon).
  </verify>
  <done>
All 5 asset files exist in src/app/. Browser tab shows custom favicon. Social preview shows OG image when URL pasted into social platform debugger.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update layout.tsx with enhanced metadata</name>
  <files>src/app/layout.tsx</files>
  <action>
Update the root layout metadata for complete SEO and social sharing:

1. Add metadataBase (REQUIRED for OG images to work):
   ```typescript
   metadataBase: new URL('https://doppeldown.com')
   ```

2. Enhance title with template:
   ```typescript
   title: {
     default: 'DoppelDown - Take Down Brand Impostors',
     template: '%s | DoppelDown'
   }
   ```

3. Add openGraph metadata:
   ```typescript
   openGraph: {
     title: 'DoppelDown - Take Down Brand Impostors',
     description: 'Detect threats. Collect evidence. Take action.',
     url: 'https://doppeldown.com',
     siteName: 'DoppelDown',
     locale: 'en_US',
     type: 'website',
   }
   ```

4. Add twitter metadata:
   ```typescript
   twitter: {
     card: 'summary_large_image',
     title: 'DoppelDown - Take Down Brand Impostors',
     description: 'Detect threats. Collect evidence. Take action.',
   }
   ```

Next.js will auto-detect the favicon/OG image files in /app directory - no explicit icon/images config needed.

Keep existing: Inter font import, body className, html lang="en".
  </action>
  <verify>
Dev server starts without errors: `npm run dev`
Inspect page source shows:
- og:image meta tag pointing to opengraph-image.png
- twitter:image meta tag pointing to twitter-image.png
- Multiple favicon link tags (ico, svg, apple-touch-icon)
  </verify>
  <done>
Layout metadata includes metadataBase, openGraph, and twitter config. Next.js auto-generates all meta tags from file-based assets.
  </done>
</task>

</tasks>

<verification>
1. Logo component renders correctly:
   - Import and render `<Logo />` - shows dark text
   - Render `<Logo mode="light" />` - shows white text
   - Render `<Logo size="lg" />` - shows larger text
   - Underscore is always primary-600 color

2. Favicon appears in browser tab (not default Next.js icon)

3. View page source shows:
   - `<link rel="icon" href="/favicon.ico"`
   - `<meta property="og:image"` with opengraph-image.png
   - `<meta name="twitter:image"` with twitter-image.png

4. Test OG image with Facebook debugger: https://developers.facebook.com/tools/debug/
</verification>

<success_criteria>
- BRAND-01: Logo component created with "doppel_down" text styling
- BRAND-02: Logo mode="light" provides white text variant
- BRAND-03: Logo mode="dark" provides dark text variant
- BRAND-04: favicon.ico, icon.svg, apple-icon.png exist and display
- BRAND-05: opengraph-image.png, twitter-image.png exist with proper metadata
</success_criteria>

<output>
After completion, create `.planning/phases/06-branding-landing-page/06-01-SUMMARY.md`
</output>
